function patata2(a, b) {
    let c = 1;

    if (a > b) {
        c = 3;
    };

    return c;
};

function fibonacci(n) {
    print(n);
    let resultado = 1;
    if (n > 2) {
        resultado = fibonacci(n - 1) + fibonacci(n - 2);
    };
    return resultado;
};

function patata() {
    let a = 'x^2 + x + 10 + 1y';
    let b = 'x + 3';

    let c = val(a, 'x', 2, 'y', 3);
};
